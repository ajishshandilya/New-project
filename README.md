# Synthx

Angular single-page website for Synthx Lubricants.

## Development

Run `npm start` and open `http://localhost:4200/`.

## Build

Run `npm run build`. The production build is generated in `dist/synthx`.
